# Copy this file to .env and fill in your values

# RDB API Configuration
# Get these values from your RDB deployment
RDB_ENDPOINT=https://your-api-gateway-id.execute-api.region.amazonaws.com/prod
RDB_API_KEY=your-api-key-here

# Chat User Configuration
# Set different usernames to simulate different users
CHAT_USERNAME=Bob
CHAT_USER_ID=user1

# Chat Filtering Configuration (for chat:listen:filtered script)
# Filter messages by chat room
FILTER_CHAT_ID=general

# Filter messages by username (optional - leave empty to see all users)
# FILTER_USERNAME=Alice